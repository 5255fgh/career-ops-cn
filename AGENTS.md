# career-ops-cn 协作规则

1. 先完成当前真实需求，只实现本次明确范围。
2. 单职位超时、解析、身份、保存或 AI 失败必须隔离并继续后续职位。
3. 登录失效、challenge 或 account risk 必须立即停止整轮。
4. 只支持 BOSS 直聘，只读运行；禁止自动投递、自动打招呼、自动聊天、联系方式操作和多平台扩展。
5. 不过早抽象，不为未来功能增加插件系统、通用注册中心、占位接口或空服务。
6. 改变行为时必须同步 README、architecture、测试和相关 fixture。
